**Avatar** — circular initials chip for reviewers in the supervision queue and audit trail. Derives initials from `name`. Use `tone="dark"` for the partner, `bone` for associates.

```jsx
<Avatar name="Partner Review" tone="dark" />
<Avatar name="Senior Associate" />
```
