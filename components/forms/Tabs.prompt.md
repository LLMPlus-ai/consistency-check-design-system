**Tabs** — underline tab strip for primary product nav and in-panel switching. Active tab gets the orange 2px underline — the one place orange appears in navigation.

```jsx
<Tabs value={tab} onChange={setTab}
  tabs={['Dashboard','Citation Checker','Supervision','Audit Trail','Data Sources']} />
```
