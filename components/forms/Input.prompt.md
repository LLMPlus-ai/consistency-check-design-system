**Input** — fully-rounded text field, white on cream, with an orange focus ring. Optional leading icon for search.

```jsx
<Input placeholder="Search citations…" iconLeft={<Icon name="search" />} />
```

Sizes: `md` (44px) · `sm` (36px). Style the inner field with `style`, the pill with `wrapStyle`.
