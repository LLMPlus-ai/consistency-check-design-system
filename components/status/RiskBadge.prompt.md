**RiskBadge** — graded risk on the Low · Medium · High · Critical ladder. `pill` for badges, `bar` for a 4-segment meter that scans fast in tables and queues.

```jsx
<RiskBadge level="Critical" />
<RiskBadge level="High" variant="bar" />
```
